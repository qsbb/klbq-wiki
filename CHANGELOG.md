# 更新日志

## 1.9.1

### Bug 修复

- **修复网络波动导致查询失败的问题**
  - `apiGet` 新增重试机制：失败后自动重试 2 次，间隔递增（1s、2s）
  - 5xx 服务器错误可重试，4xx 客户端错误不重试
  - 网络错误（fetch failed、超时）自动重试
  - 重试失败后记录最终错误日志

- **改进错误提示，不再返回"查询失败"技术错误**
  - `handleSkin` 获取角色页面失败时返回"网络错误，可能是网络波动，请稍后重试"
  - `handleSkin` 无皮肤数据时返回"无皮肤数据"提示
  - `handleBirthday` 无数据时返回"暂无数据，请稍后重试"
  - `handleCatLanguage` 无数据时返回"暂无数据，请稍后重试"
  - 这些场景之前会 throw 导致 `handleQuery` 返回"查询失败，已写入后台日志"的不友好提示

### 问题背景

用户反馈 `-玛德蕾娜失落的伊卡洛斯` 查询失败，日志显示：
```
[KlbqWiki] API 请求失败: TypeError: fetch failed
[KlbqWiki] 查询异常: query=玛德蕾娜失落的伊卡洛斯, error=Error: 无法获取"玛德蕾娜·利里"角色页面
```

原因是 Wiki API 请求出现瞬时网络故障（fetch failed），`queryPageHtml` 返回 null，`handleSkin` 直接 throw 导致查询失败。现在增加了重试机制和友好的错误提示。

## 1.9.0

### 架构重构（参考 miao-plugin 设计）

- **别名配置改为 YAML 文件（支持热更新）**
  - 新增 `defSet/aliases.yaml`：默认别名配置（随插件更新）
  - 新增 `config/aliases.yaml`：用户自定义别名（覆盖默认，无需重启）
  - 格式参考原神插件的 `name.yaml`：`角色名: [别名1, 别名2, ...]`
  - 通过 chokidar 监听文件变更，修改后自动重新加载
  - 向后兼容：旧的 `config.yaml` 的 `custom_aliases` 仍可用
  - 别名解析全部小写存储，查询时小写化，英文别名不区分大小写

- **命令分派改为正则规则数组**
  - 新增 `_dispatchRules` 数组：每条规则包含 `reg`（正则）和 `fn`（处理函数）
  - 按顺序匹配，命中即返回，逻辑更清晰易扩展
  - 参考 Yunzai 自带 genshin 插件的 `rule` 数组设计
  - 新增 `trySkinDispatch` 方法：封装皮肤前缀匹配逻辑

- **保留皮肤前缀匹配（我们的优势功能）**
  - 原神插件不支持皮肤名前缀剥离，我们保留并优化了这个功能
  - 支持无空格皮肤查询：`-心夏私服`、`-糖猫猎虎裁恶`、`-明危险游戏`
  - 单字符角色名（明/信/令/汐）支持前缀匹配
  - 长别名优先匹配（如"哈基米雪儿"优先于"哈基米"）

### 文件变更

- 新增 `defSet/aliases.yaml`：默认别名配置（24 个角色，116 个别名）
- 重写 `lib/aliases.js`：从 YAML 加载，支持 chokidar 热更新
- 重构 `index.js`：`handleQuery` 改为正则规则数组分派
- 更新 `README.md`：新增 YAML 别名配置说明

### 测试覆盖

46 项测试全部通过，覆盖：
- YAML 别名加载（角色名本身 + 别名 + 英文大小写）
- YAML 文件结构验证
- 用户配置覆盖机制
- 向后兼容（buildAliasMap）
- 命令分派规则结构
- 皮肤前缀匹配回归测试（11 个用例）

## 1.8.6

### Bug 修复

- **修复 `-明危险游戏` 等单字符角色名+皮肤名的无空格查询失败的问题**
  - 单字符角色名（明、信、令、汐）现在也支持前缀匹配具体皮肤名
  - 例如：`-明危险游戏`、`-令xxx`、`-汐xxx` 均可查询
  - 皮肤部分仍要求至少 2 个字符，避免误匹配

## 1.8.5

### Bug 修复

- **修复 `-糖猫猎虎裁恶`、`-心夏机动天使`、`-糖猫踏雪飞猫` 等无空格皮肤查询失败的问题**
  - 现在支持 `角色别名+具体皮肤名` 的无空格写法
  - 遍历别名表做前缀匹配，按别名长度降序优先匹配长别名（如"哈基米雪儿"优先于"哈基米"）
  - 支持的示例：
    - `-糖猫猎虎裁恶`（糖猫 = 米雪儿）
    - `-心夏机动天使`
    - `-糖猫踏雪飞猫`
    - `-心夏私服`、`-米雪儿宿舍皮`、`-信私皮` 等
  - 安全机制：前缀匹配具体皮肤名时要求角色部分和皮肤部分都至少 2 个字符，避免误匹配

## 1.8.4

### Bug 修复

- **修复 `-心夏私服` 等无空格皮肤查询失败的问题**
  - 现在支持 `角色名+私服/宿舍皮/私皮/皮肤` 的无空格写法
  - 仅当角色名在别名表中已知时才分派到皮肤查询，避免误吞普通条目查询
  - 例如：`-心夏私服`、`-米雪儿宿舍皮`、`-信私皮` 均可直接查询

- **修复查询失败时发送搜索链接的问题**
  - 查询不到条目时不再回复搜索链接
  - 改为提示用户检查名称或使用 `-帮助` 查看支持的查询

## 1.8.3

- **`-活动` 布局优化：活动数大于 4 时每行两个**
  - 4 个及以下：单列布局，每个活动卡片较大（图片 360px、标题 24px）
  - 5 个及以上：双列网格布局，紧凑显示（图片 200px、标题 20px、类型标签缩小）
  - 避免活动过多时图片过长

## 1.8.2

- **`-活动` 命令改为图片卡片发送**
  - 不再发送链接，不再逐张发图
  - 以单张图片卡片展示所有正在进行的活动
  - 每个活动包含：类型标签、标题、活动图片、起止时间、剩余时间、进度条
  - 3 天内结束的活动进度条标红
  - 图片渲染失败时回退文字格式

- **新增 `resources/activities.html` 模板**
  - 紫色渐变背景，与其他卡片风格一致
  - 每个活动独立卡片，包含图片和详情

## 1.8.1

- **`-日历` 优化：只显示当月过生日的角色**
  - 按日期升序排列，当月已过生日的角色灰显并标注"已过 N 天"
  - 今天过生日的角色高亮显示
  - 副标题显示"共 X 个本月生日（X月）"
  - 本月无角色生日时显示"X月无角色生日"

- **新增 `-活动` 命令：显示当前正在进行的活动图片**
  - 自动筛选正在进行中（已开始且未结束）的活动
  - 先发送文字摘要（活动列表 + 倒计时 + 详情链接）
  - 再逐个发送活动图片（通过图片缓存，本地优先）
  - 支持别名：`-当前活动`

- **`wiki.calendarEvents()` 返回值扩展**
  - 新增 `image` 字段：活动卡片图片 URL
  - 新增 `url` 字段：活动详情页 URL

## 1.8.0

- **新增 `-日历` 命令：可视化展示活动倒计时与角色生日**
  - 自动获取 Wiki 首页所有倒计时事件（赛季、活动、奖池等）
  - 每个事件显示类型标签、标题、起止时间、剩余天数、进度条
  - 3 天内结束的事件标记为"紧急"（红色高亮）
  - 已结束的事件灰显并排到末尾
  - 下方展示最近 8 个角色生日，今天过生日的角色高亮显示
  - 支持别名：`-活动日历`、`-倒计时`
  - 图片渲染失败时回退文字格式

- **新增 `wiki.calendarEvents()` 方法**：获取首页所有 `.eventTimer` 倒计时
  - 解析 `data-info`（类型）、`data-end`（结束）、`data-start`（开始）属性
  - 自动查找所属卡片的标题
  - 计算剩余天数、总天数、进度百分比
  - 按结束时间升序排序（已结束的排末尾）

- **新增 `parseDate(raw)` 辅助函数**：解析 Wiki 日期字符串
  - 支持 `2026/7/30 05:59` 和 `2026-09-15 05:59` 两种格式
  - 空值返回 null

- **新增 `resources/calendar.html` 模板**
  - 时间轴式倒计时列表（类型标签 + 标题 + 进度条 + 倒计时）
  - 生日列表（名字 + 日期 + 倒计时）
  - 紫色渐变背景，与其他卡片风格一致

## 1.7.2

- **优化更新命令的消息展示**
  - **解析 git 输出为友好格式**：之前直接显示 `Fast-forward` 和文件变更列表原始文本，现在解析后展示：
    - 提交哈希范围（如 `a06ae30 → 72374a3`）
    - 变更文件数（如 `5 个（+41 / -7）`）
    - 变更文件列表（最多 10 个，超出显示"等共 N 个文件"）
  - **合并为单条消息**：之前更新成功和重启提示分两条消息发送，现在合并为一条
  - **移除冗余重启说明**：不再发送"重启机制：通过 redis 标记 + process.exit 实现"等技术细节
  - 消息以图标开头（✅📦🔄📄📝），更直观易读
- **新增 `parseGitPullOutput(text)` 辅助函数**：解析 git pull 的中英文输出

## 1.7.1

- **修复 puppeteer 无法加载本地缓存图片的问题**
  - 根因：`ImageCache` 返回相对路径（`plugins/klbq-wiki/data/images/...`），但 puppeteer 用 `page.setContent()` 加载 HTML 时基础是 `about:blank`，相对路径无法解析
  - 修复：`CACHE_DIR` 改为绝对路径（`path.resolve(process.cwd(), ...)`），`get()` 返回绝对路径
  - 修复：puppeteer 的 `<img src>` 必须用 `file://` URL 才能加载本地文件，新增 `toFileUrl()` 函数转换
  - `renderImage` 的 `thumb` 和 `handleBirthday` 的 `hero.art` 传入模板前用 `toFileUrl()` 转换
  - `segment.image()` 仍用绝对路径（Yunzai 原生支持）
- **影响范围**：`-生日` 卡片的立绘、`-心夏` 等角色卡片的立绘现在可以正确显示

## 1.7.0

- **新增 `-更新资源` 命令：预下载全部角色立绘和皮肤图到本地**
  - 解决图片加载不出来的问题：用户可主动触发批量下载
  - 自动遍历 Wiki 所有超弦体角色，下载每个角色的立绘和全部皮肤图
  - 下载的图片存到 `data/images/`，之后查询直接读取本地文件
  - 支持别名：`-卡拉彼丘更新资源`、`-缓存资源`、`-预下载`
  - 仅主人可用
- **进度反馈**：执行期间每 5 个角色或 15 秒汇报一次进度，避免长时间无响应
- **完成统计**：显示角色数、立绘数、皮肤数、成功/失败数、耗时、新增文件数和大小
- **容错机制**：单个角色或图片失败不影响整体，中断后可重新执行继续下载
- **新增 `wiki.fetchAllResources(hooks)` 方法**：批量预下载，支持 onStart/onRole 回调
- **新增 `ImageCache.stats()` 方法**：统计缓存文件数和总大小
- **新增 `formatBytes()` 辅助函数**：字节数格式化为人类可读字符串（1 KB / 1.5 MB）
- 缓存未启用时友好提示用户先开启 `image_cache`

## 1.6.0

- **新增图片缓存功能，解决角色图片加载不出来的问题**
  - 查询过的角色立绘、皮肤图会缓存到本地 `data/images/` 目录
  - 下次查询同一图片直接读取本地文件，不再重复网络下载
  - 适用于所有图片场景：角色卡片立绘、皮肤详情图、生日卡片立绘
  - 图片 URL 经 MD5 哈希作为本地文件名，自动推断扩展名
  - 支持批量并发下载（`getMany`，默认并发 4）
- **新增配置项**
  - `image_cache`（默认 `true`）：图片缓存开关
  - `image_cache_ttl`（默认 `30`，范围 0-365 天）：缓存有效期，0 表示永不过期
  - 可通过 `-设置 image_cache off` 关闭缓存
  - 可通过 `-设置 image_cache_ttl 0` 设为永不过期
- **容错机制**
  - 缓存目录创建失败时自动降级为直接使用远程 URL
  - 单张图片下载失败时回退到远程 URL，不影响其他图片
  - `logger` 全局变量不存在时静默处理（测试环境兼容）
- **`.gitignore` 新增 `data/` 规则**：缓存数据不纳入版本控制
- **新增 `lib/image-cache.js`**：独立的图片缓存模块，可复用

## 1.5.0

- **生日卡片改版：展示最近生日角色的随机立绘 + 名字更显眼**
  - 新增 `resources/birthday.html` 生日专用模板
  - 顶部突出展示最近一位生日角色：大尺寸立绘 + 64px 加粗名字 + 日期 + 倒计时
  - 下方列表展示其他近期生日角色
  - 名字使用 64px 字号、900 字重、文字阴影，视觉上更突出
- **新增 `wiki.getCharacterArt(title)` 方法**
  - 通过 Wiki 分类查询直接获取角色立绘 URL（不依赖页面 HTML）
  - 随机返回一张立绘（多张立绘的角色每次查询可能不同）
  - 自动排除生日图、壁纸、表情、模型、头像、图标、武器、皮肤、宿舍、CG 等非立绘图
- **倒计时优化**：新增"明天"显示（之前只有"今天"和"还有 N 天"）
- **容错机制**：立绘获取失败时仍正常渲染卡片（不显示立绘区域）；图片渲染失败时回退文字

## 1.4.1

- **设置卡片配置项改为中文显示**
  - 每个 CONFIG_META 配置项新增 `label` 字段（中文显示名）
  - 设置卡片显示格式：`中文label（英文key）`，如 `图片渲染（render_image）`
  - 英文 key 仍用于 `-设置 <key> <值>` 修改命令
- **修复设置卡片"使用方法"分组显示奇怪字符的问题**
  - 根因：代码中手动调用 `escapeHtml` 转义后，art-template 的 `{{}}` 又自动转义一次，导致双重转义
  - `<项名>` 被转义成 `&lt;项名&gt;` 后又被转义成 `&amp;lt;项名&amp;gt;`，图片上显示为 `&lt;项名&gt;` 字面字符
  - 修复：移除 `renderImage` 和 `renderHelp` 中所有手动 `escapeHtml` 调用，统一由 art-template 自动转义
  - 修复后 `<项名>` `<值>` 等尖括号内容正确显示

## 1.4.0

- **帮助和设置改为分组图片卡片**
  - 新增 `resources/help.html` 分组卡片模板，参考 Yunzai 帮助风格
  - 每个分组独立区块展示，组内每条指令一行，清晰易读
  - 帮助卡片按"角色与武器 / 皮肤 / 其他 / 插件管理 / 命令前缀"分组
  - 设置卡片按"功能开关 / 插件更新 / 查询设置 / 图片布局 / 使用方法"分组
- **`-设置` 命令改为图片发送**
  - 之前为纯文字列表，现在渲染为分组图片卡片
  - 每个配置项显示名称、说明和当前值
  - 图片渲染失败时自动回退文字
- **`-帮助` 命令改为分组图片**
  - 之前为单段长文本，现在按分组区块展示
  - 每条指令独立一行，命令与说明清晰对应
- 新增 `renderHelp(title, kind, groups)` 方法渲染分组卡片
- 新增 `sendHelp(e)` 方法发送帮助卡片（含文字回退）
- `helpText()` 改为 `helpData()` 返回分组数据结构
- 渲染失败时自动回退文字，保持可用性

## 1.3.3

- 新增 `-喵` 作为 `-喵言喵语` 的简写命令
  - 所有前缀同步支持：`-喵`、`#klbq喵`、`#klbq 喵`、`#卡拉彼丘喵`、`#卡丘喵` 等
  - 前缀与"喵"之间有无空格均可
  - 与 `-喵言喵语`、`-随机喵言喵语` 效果相同
  - 已确认不与角色别名冲突（别名表中无"喵"角色）

## 1.3.2

- 修复 `#klbq心夏`、`#卡拉彼丘心夏`、`#卡丘心夏` 等无空格前缀写法无法触发查询的问题
  - 原因：主正则 `(?:\s|$)` 要求前缀后必须有空白或字符串结尾，导致 `#klbq` 紧跟中文时不匹配
  - 修复：移除 `(?:\s|$)` 限制，前缀后紧跟关键词也可识别
  - 同步修复 `extractPrefix` 函数的 `(?=\s|$)` 前瞻，确保无空格写法的提示前缀也正确
- 现在前缀与关键词之间有无空格均可识别：
  - `#klbq 心夏` 和 `#klbq心夏` 均可
  - `#卡拉彼丘 心夏` 和 `#卡拉彼丘心夏` 均可
  - `#卡丘 心夏` 和 `#卡丘心夏` 均可
  - `/` 前缀同理

## 1.3.1

- 修复 `-帮助` 等单条内容图片卡片右侧大片空白的问题
  - 原因：当 items 数量少于配置的列数时，单条内容只占左半边
  - 修复：`renderImage` 根据 items 数量动态调整实际列数（`min(columns, items.length)`）
  - 影响：帮助、赛季、生日等单条内容卡片现在占满整行
- 修复角色/武器卡片底部提示硬编码 `#klbq` 的问题
  - 现在提示前缀跟随用户发送的指令
  - 例如 `-心夏` → `提示：可继续使用 - 空境 查询心夏的武器`
  - 例如 `#卡拉彼丘 心夏` → `提示：可继续使用 #卡拉彼丘 空境 查询心夏的武器`
- 修复皮肤列表卡片提示硬编码 `#klbq` 的问题
  - 现在同样跟随用户发送的指令前缀

## 1.3.0

- 新增 `-卡拉彼丘更新` / `-卡拉彼丘强制更新` 自动重启功能
  - 更新成功后自动重启 Yunzai，无需手动操作
  - 采用 Yunzai 官方重启机制：redis 设置 `Yz:restart` 标记 + `process.exit`
  - 依赖 PM2 等进程管理器自动拉起新进程
  - 默认延时 3 秒后重启，确保消息发送完成
- 新增配置项 `auto_restart`（默认 `true`）：可关闭自动重启改为手动
- 新增配置项 `restart_delay`（默认 `3`，范围 1-30）：重启前等待秒数
- 可通过 `-设置 auto_restart off` 关闭自动重启
- 可通过 `-设置 restart_delay 5` 修改等待时间

## 1.2.0

- 新增 `-设置` 命令：在机器人内交互式查看和修改配置（仅主人）
  - `-设置` 查看所有配置项
  - `-设置 <项名> <值>` 修改配置并自动保存
  - `-设置 <项名> on/off` 布尔项快捷开关
  - `-设置 重置` 恢复全部默认配置
  - 支持模糊匹配配置项名（如 `-设置 send` 匹配 `send_detail_link`）
  - 数字项自动范围校验
  - 修改后自动保存到 config.yaml，重启后完全生效

## 1.1.1

- 修复图片发送失败 `base64://[object Object]` 问题
  - 原因：Yunzai 的 `puppeteer.screenshot` 已返回 segment 对象，原代码二次包装导致失败
  - 修复：直接使用返回的 segment 对象，不再二次包装
- `send_detail_link` 默认值改为 `false`
  - 原因：单独发送 Wiki 链接会触发其他插件（如 lin-plugin 复读只因）的 bug
  - 图片卡片已包含完整资料，链接非必需。如需开启请手动设置为 true

## 1.1.0

- 新增 `-卡拉彼丘更新` 命令：从 GitHub 拉取插件最新版本（仅主人）
- 新增 `-卡拉彼丘强制更新` 命令：丢弃本地改动并强制更新（仅主人）
- 支持简写：`-更新`、`-强制更新`
- 内置常见错误诊断（本地改动冲突、分支分歧、认证失败、超时等）
- 修复 puppeteer 渲染中文标题时 ERR_FILE_NOT_FOUND 的问题

## 1.0.0

- 首次发布 Yunzai 版本
- 从 astrbot_plugin_klbq_wiki v1.4.5 完整移植
- 同时支持 `-`、`#klbq`、`/klbq`、`#卡拉彼丘`、`/卡拉彼丘`、`#卡丘`、`/卡丘` 多种命令前缀
- 例如：`-心夏`、`#klbq 心夏`、`#卡丘 心夏 皮肤`
- 支持角色、武器、角色武器、皮肤、生日、赛季、喵言喵语查询
- 使用 Yunzai 内置 puppeteer 渲染图片卡片
- 使用合并转发消息发送皮肤图片
- 使用 cheerio 替代 BeautifulSoup 进行 HTML 解析
- 使用 YAML 配置文件替代 AstrBot WebUI 配置
